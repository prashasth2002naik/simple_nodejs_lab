apiVersion: v1
kind: Service
metadata:
  labels:
    app: monitorapp
  name: pythonmonitor
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: monitorapp
